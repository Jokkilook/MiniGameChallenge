# 총 메시 원본(.glb) — 텍스처 때문에 6MB 가 넘는다.
# 게임이 읽는 건 구워진 gunbaked.js(190KB) 뿐이라 원본은 올리지 않는다.
# 모델을 바꿀 때만 필요하다 — README 의 "3D 메시 넣기" 참고.
Mesh/

# 아이템 아이콘 원본(.png) — 장당 1.4MB 라 6장이면 9MB 다.
# 게임이 읽는 건 구워진 itemicons.js(105KB) 뿐이라 원본은 올리지 않는다.
# 그림을 바꿀 때만 필요하다 — README 의 "HUD 아이콘 굽기" 참고.
Image/

# 미리보기 서버 실행 설정(개인 환경)
.claude/
